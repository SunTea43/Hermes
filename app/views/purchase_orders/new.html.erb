<% content_for :title, "Nueva orden de compra" %>

<%= render PageHeaderComponent.new(title: "Nueva orden de compra") %>

<%= render CardComponent.new(css_class: "col-lg-8") do %>
  <%= render "form", purchase_order: @purchase_order %>
<% end %>
