<% content_for :title, "Editar orden de compra" %>

<%= render PageHeaderComponent.new(title: "Editar orden de compra") %>

<%= render CardComponent.new(css_class: "col-lg-8") do %>
  <%= render "form", purchase_order: @purchase_order %>
<% end %>
