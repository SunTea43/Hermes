<% content_for :title, "Nuevo inventario" %>

<%= render PageHeaderComponent.new(title: "Registrar inventario manual") %>

<%= render CardComponent.new do %>
  <%= render "form", inventory: @inventory %>
<% end %>
