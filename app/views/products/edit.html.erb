<% content_for :title, "Editar #{@product.name}" %>

<%= render PageHeaderComponent.new(title: "Editar: #{@product.name}") %>

<%= render CardComponent.new do %>
  <%= render "form", product: @product %>
<% end %>
